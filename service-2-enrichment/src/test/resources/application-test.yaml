server:
  port: 8081

spring:
  application:
    name: enrichment-service-test
  
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    root: WARN
    com.example.enrichment: DEBUG
